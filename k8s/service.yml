apiVersion: v1
kind: Service
metadata:
  name: node-app-service
  namespace: webapps
spec:
  type: NodePort
  selector:
    app: node-app
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30003
